{
  "name": "gwt-monorepo",
  "private": true,
  "version": "0.1.7",
  "workspaces": ["packages/*"],
  "scripts": {
    "check": "cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && cargo test --all",
    "pack:check": "cargo xtask pack-check",
    "versions:check": "cargo xtask verify-versions"
  }
}
